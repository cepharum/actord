{
	"name": "pulling-deployer",
	"version": "0.0.1",
	"description": "web service exposing hook for triggering pulls for deploying application",
	"main": "lib/index.js",
	"scripts": {
		"start": "node_modules/.bin/hitchy start --log-level=hitchy:request,pulling-deployer:*",
		"dev": "node_modules/.bin/hitchy start --debug",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "https://git.cepharum.de/hosting/pulling-deployer.git"
	},
	"author": "",
	"license": "MIT",
	"dependencies": {
		"file-essentials": "^0.0.12",
		"hitchy": "^0.0.16"
	}
}
